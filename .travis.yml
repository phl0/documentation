before_install:
- sudo apt-get update 
- sudo apt-get install texlive-latex-extra
script:
- mkdir _build
- pdflatex -output-directory _build ./DAPNET\ 2.0\ Software\ internals.tex
branch: travis_test
